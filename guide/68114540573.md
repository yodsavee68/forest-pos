# งานของคุณ: นายวิศรุต ปู่แก้ว (68114540573)

**Role:** Waiter / Clear ออเดอร์และการแจ้งเตือน  
**หน้าที่หลัก:** จัดการสถานะออเดอร์ (เช่น เสิร์ฟแล้ว) และระบบแจ้งเตือนพนักงาน

---

## 1. สิ่งที่ต้องทำ (Checklist)

### Backend (FastAPI)
- [ ] **Update Model/Schema:** ตรวจสอบ Model `OrderItem` ใน `server/app/models/order.py` (สถานะอยู่ที่ Item ไม่ใช่ Order ใหญ่เสมอไป หรือแล้วแต่ดีไซน์)
- [ ] **API Router:** ใช้งาน/สร้างต่อใน `server/app/api/orders.py` หรือ `notifications.py`
- [ ] **Implement Routes:**
    1. `PATCH /api/orders/{order_id}/items/{item_id}/status` หรือ `.../status`
       - รับ status ใหม่ (เช่น `served`, `cancelled`)
       - อัพเดทลง `OrderItem` หรือ `Order` ตาม business logic
       - (สำคัญ) ส่ง Socket.io event เพื่อบอกหน้าจอ Admin/Customer ว่าสถานะเปลี่ยนแล้ว
    2. `POST /api/notifications/send`
       - รับ request เรียกพนักงาน (Call Waiter)
       - บันทึก log หรือส่ง Socket ไปที่เครื่องพนักงาน

### Frontend (Next.js)
- [ ] **Waiter Dashboard (Mobile view):** หน้าสำหรับพนักงานกดรับทราบ หรือกดว่าเสิร์ฟแล้ว
- [ ] **Customer UI:** ปุ่ม "เรียกพนักงาน" (Call Staff) บนหน้าโต๊ะลูกค้า

---

## 2. คำแนะนำทางเทคนิค (Technical Guide)

### Socket.io Integration
ในไฟล์ `server/app/main.py` หรือ module แยก:
```python
# ตัวอย่างการส่ง event เมื่ออัพเดทสถานะ
@router.patch("/orders/{order_id}/status")
async def update_status(order_id: int, status: str):
    # ... update db logic ...
    await sio.emit('order_status_updated', {'order_id': order_id, 'status': status})
    return {"message": "Status updated"}
```

### การ test
- เปิด 2 หน้าจอ (Customer, Waiter)
- กด "เรียกพนักงาน" จาก Customer -> Waiter ต้องเห็นแจ้งเตือนทันที (Real-time)

---

## 3. การใช้งาน Git (Git Workflow)

1. **สร้าง Branch ใหม่:** ตั้งชื่อว่า `dev-<ชื่อเล่น>`
   ```bash
   git checkout -b dev-yourname
   ```
2. **ก่อนเริ่มงาน:** ดึงโค้ดล่าสุดจาก `main` เสมอ
   ```bash
   git checkout main
   git pull origin main
   git checkout dev-yourname
   git merge main
   ```
3. **ส่งงาน (Pull Request):**
   - Push branch ขึ้น Server
     ```bash
     git push origin dev-yourname
     ```
   - เข้า Web GitHub ไปกด New Pull Request
   - **สำคัญ:** แจ้ง Leader (หรือเพื่อนในทีม) ให้มาตรวจโค้ดทุกครั้งก่อนรวม
